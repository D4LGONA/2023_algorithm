파이썬에 대해 알아보는 시간 - 투디겜플 강의자료를 쓰시는군
계속 발전하고 있어요 쓸만한 걸 바로바로 사용
장단점...
인기가 정말 많아요

키워드들
파이썬은 대소문자 구분을 합니다. 대문자인건 True False None 이 세개에요
not, or, and ... 
else if를 elif로 씁니다
함수정의 def(c에선 ()가 함수.. 인거임)
모듈? import, from
예외처리 try except raise finally
return, yield(잠깐 쓰고 다시 진입..?)

IDLE : 에디터이자 실행 환경이자...
REPL? Read, Eval, Print Loop 아하.. idle를 실행하면 나오는 것을 말하는군
자바스크립트도 REPL을 지원한다고 하네요 node.js?
스위프트는 컴파일러 언어인데도 REPL을 지원해요

연산자들(+, -, *, /(실수나누기) //(정수나누기), **(제곱), %(나머지 - 실수도 가능))

IDLE단축키 alt+p alt+n
변수, print()
변수에 함수를 담을 수 있다는데요

문자열
"" '' 둘다 되는거 -> 이스케이프 안해도 되게 하려고.. 요즘 트렌드임
곱셈이랑 덧셈
input은 무조건 문자열로 취급해요
type()
int(), str() 이런걸로 냅다 자료형 변환 가능 얘가 함수가 아니고 int 객체가 생성되는거라네요 가 아니라 둘다 똑같은거래요
함수호출 == 객체생성 이라는데요

bool

문자열 비교 : 대소문자 구별, 따옴표 상관 없음
collection이어서 할 수 있는 다양한 연산들(문자열, 리스트, 집합, 딕셔너리, 튜플... 모두 포함)
폴.. 폴리모르피즘(다형성)
객체지향 개발 방법론

슬라이싱

list ...  a.len()을 안쓰고 len(a)을 쓰는게 유물이라네요
리스트 슬라이스
리스트 특징: value들을 쭉 나열한것. 특정 값을 가져오려면 인덱스를 주고 가져옴 인덱스는 정수

딕셔너리: 정수가 아닌것을 인덱스로 쓴다는 느낌 주로 문자열로 써요
key - value, key in dict
아니 얘가 해시를 이용한 거라고? 얘가 해시 테이블이라고? 충격이다... 자료구조 왜 배움? 파이썬에 다 있는데ㅋㅋ
.keys(), .values(), .clear()

튜플: 두개 이상의 값을 묶어서 하나의 변수에 대입하는 것
승민이가 저번 학기에 그렇게 말했던 게 이 녀석이군 함수 리턴할때 리턴할게 많으면 튜플로 묶어서 리턴해요
콤마가 대입연산자보다 우선순위가 높아요
소괄호를 쓰라네요

set: 집합. 중복허용 x, 순서 x
| 합집합, & 교집합, - 차집합, add, remove
집합에 원소가 있는지 확인하기 위해 쓴다구? value 없는 딕셔너리 느낌 얘도 해시를 써서 빨리 찾는대요

위에서부터 아래로 차례대로 실행... 당연한거 아닌가요?
분기 / 반복
코드블럭을 들여쓰기로 표현 들여쓰기가 정말 중요해!!!
탭 들여쓰기랑 빈칸 네개를 구별하나봐.... 아 같은게 들어있으면 같은 코드블럭인거네 그냥
무조건 : 를 써야 함

-- 실습

p01_list: range, print는 무조건 endline을 출력하고 print 안에 end=''이런식으로 하면 endline을 안할수있음
len()이나 range나 똑같다는데
꼭 리스트를 써야하면 list()를 쓰세요

p01_list_slicing: 거의 [inclusive, exclusive)를 사용해요
randint만 [inclusive, inclusive]라네요

p02 dictionary: 중괄호와 : 를 사용하여 정의함 {key : value}
키가 존재하는지 확인하도록 해요
파이썬엔 3항연산자가 없군 a ? b : c를 b if a else c 로 쓴다는데요

p01 dictionary usage: 단어 세기 프로그램 -> 첫번째 글자와 개수로 딕셔너리를 ... ㅠ
sorted? 키가 추가된 순서대로 딕셔너리가 생성됨
defaultdict? 없으면 딕셔너리에 넣어준다는 말 같은데 defaultdict(int) 하면 없을때 int()가 호출돼서 0이 해당 키값으로 저장된다는듯
defaultdict 클래스를 가져와야해요 collections라는 모듈에 들어 잇어
from collections import defaultdict














